

[defaults]
inventory            = inventory/hosts.yml
host_key_checking    = False
remote_user          = rocky
interpreter_python   = auto_silent
stdout_callback      = yaml
deprecation_warnings = False
become_password_file = ~/.vault_pass     ← ajoute cette ligne

[ssh_connection]
pipelining = True